# Task: ImGui 기반 기본 UI 프레임워크 확보

## Purpose

Dear ImGui를 도입해 기본적인 UI 프레임워크(코드/빌드 통합)를 확보한다.
이번 사이클의 목표는 "프레임워크 확보" 그 자체이며, `main.cpp`에 실제 UI(메뉴/HUD 등 게임 콘텐츠성 UI)를 만드는 것은 범위에 포함하지 않는다.

## Author

(비워둠)

## User Draft

- ImGui를 사용하되, 실질적으로 `main.cpp` 안에 ImGui로 뭔가를 그리는(UI 콘텐츠를 만드는) 작업은 하지 않는다.
- 이번 사이클은 코드 및 빌드 프레임워크 확보(라이브러리 통합, wrapper 구조, 빌드 연결)까지가 목표.

## Requirements

(미정 — 브레인스토밍에서 확정, 예: 어떤 렌더러 백엔드까지 이번 사이클에서 ImGui를 연결할지, 프레임워크 동작을 어떻게 검증할지 등)

## Checklist

(미정 — 브레인스토밍/전략 단계에서 세분화)
