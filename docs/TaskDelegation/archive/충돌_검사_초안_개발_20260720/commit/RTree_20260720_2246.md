../strategy/RTree_20260720_2246.md

# Commit: R-Tree

## 구현 결과

- `src/collision/rtree/RTree.h`/`.cpp` — Guttman 방식 고전 R-Tree: `Insert()`로 리프까지 내려가며 삽입(`ChooseSubtree`로 부피 증가 최소 자식 선택), 노드당 항목 수 초과 시 quadratic split, 루트 분할 시 트리 높이 증가.
- `FindOverlappingPairs()` — 리프 노드는 브루트포스 쌍 비교, 내부 노드는 자식별 재귀 + 서로 다른 자식 쌍마다 `CollectOverlaps`(형제가 항상 같은 높이라는 R-Tree 불변식 이용).
- 리프-리프 최종 판정은 기존 `geometry/Intersections.h`의 `Intersects(AABB, AABB)` 재사용.
- `CMakeLists.txt`: `app_lib`/`tests`에 각각 `.cpp` 추가.
- `tests/RTreeTest.cpp`: BVH/SAP와 동일 배치 검증 + 분할 유발(10개 분리 객체) + 분할 후 정확성(6개 겹치는 객체, C(6,2)=15쌍) + 생성자 이후 `Insert()` 증분 삽입 케이스, 총 5개.
- GJK/EPA/SweepAndPrune/BVH 등 다른 알고리즘 모듈을 전혀 참조하지 않음(독립성 확인).

## 현재 상태에서 확인된 이슈

- [RESOLVED] `Win32WindowTest.PumpMessagesReturnsTrueWhenNoQuitPosted` 1건 실패 — 이전 사이클들에서도 동일하게 보고된 기존 이슈, 이번 사이클과 무관.
  → resolved: `tests/Win32WindowTest.cpp`에 픽스처 추가, SetUp()에서 잔여 WM_QUIT 드레인. 227/227 테스트 통과 확인 (2026-07-22).

## 체크리스트/검증 상태

전략 문서(`../strategy/RTree_20260720_2246.md`)의 체크리스트 4개 전부 `[x]`, 승인/검증 기준 3개 전부 충족.

## 다음 단계

코드 리뷰는 생략(로드맵 10번까지 완료 후 일괄 진행), 로드맵 6번(Voxel 공간 / Spatial Hashing)으로 진행.
