../strategy/MPR_20260720_2259.md

# Commit: MPR

## 구현 결과

- `src/collision/mpr/Mpr.h`/`.cpp` — `bool MprIntersects(a, b)`: 내부 기준점(v0) 산정 → 초기 포탈(v1,v2) 탐색 → 광선 브라케팅 보정 → 포탈 반복 정제(XenoCollide 방식)로 boolean 충돌 판정.
- 함수명을 `Intersects`가 아니라 `MprIntersects`로 둠 — `GjkEpa.h`가 이미 전역에 동일 시그니처(`Intersects(Geometry,Geometry)`)를 정의하고 있어 이름이 같으면 `app_lib` 링크 단계에서 중복 정의 오류가 났음(구현 중 실제로 발견해 수정).
- `CMakeLists.txt`: `app_lib`/`tests`에 각각 `.cpp` 추가.
- `tests/MprTest.cpp`: Sphere-Sphere(분리/충돌/접촉) + AABB-AABB(분리/충돌) 총 5개 boolean 케이스.
- GJK/EPA 등 다른 알고리즘 모듈을 전혀 참조하지 않음(독립성 확인).

## 현재 상태에서 확인된 이슈

- [DEFERRED] OBB처럼 지지 함수가 한 옥탄트 전체에서 같은 모서리를 반환하는 평평한(polytope) 도형에서, 포탈 정제가 수렴하지 않고 몇 개의 정점 사이를 순환하는 경우를 발견(Sphere-OBB 조합으로 추가 검증 중 확인). 전략 문서에 명시된 Sphere/AABB 범위 밖의 발견이라 이번 사이클 수정 대상에서 제외하고 기록만 함 — 참고용/낮은 우선순위 모듈이라 추가 조사는 다음 사이클로 미룬다.
- [RESOLVED] `Win32WindowTest.PumpMessagesReturnsTrueWhenNoQuitPosted` 1건 실패 — 이전 사이클들에서도 동일하게 보고된 기존 이슈, 이번 사이클과 무관.
  → resolved: `tests/Win32WindowTest.cpp`에 픽스처 추가, SetUp()에서 잔여 WM_QUIT 드레인. 227/227 테스트 통과 확인 (2026-07-22).

## 체크리스트/검증 상태

전략 문서(`../strategy/MPR_20260720_2259.md`)의 체크리스트 4개 전부 `[x]`, 승인/검증 기준 2개 전부 충족(범위 내). OBB 한계는 [DEFERRED]로 별도 기록.

## 다음 단계

코드 리뷰는 생략(로드맵 10번까지 완료 후 일괄 진행), 로드맵 9번(Conservative Advancement CCD)으로 진행.
