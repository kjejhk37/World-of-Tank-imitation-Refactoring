../strategy/FCL_20260720_2315.md

# Commit: 커스텀 FCL 통합 파이프라인

## 구현 결과

- `src/collision/fcl/Fcl.h`/`.cpp` — `class Fcl`: 생성자에서 Geometry 목록을 받아 저장, `DetectCollisions()`가 `Bvh`로 broad-phase 후보 쌍을 추리고 각 후보에 `ComputePenetration()`(GJK+EPA)을 적용해 실제 충돌만 침투 정보와 함께 반환.
- 요구사항 1(모든 충돌 감지 알고리즘은 서로 독립)의 명시적 예외로 `Bvh`/`GjkEpa` 두 모듈만 참조 — 브레인스토밍에서 합의된 대로 로드맵 마지막 항목으로 구현.
- `CMakeLists.txt`: `app_lib`/`tests`에 각각 `.cpp` 추가.
- `tests/FclTest.cpp`: broad-phase 오탐(AABB는 겹치지만 실제 구는 안 닿음) 필터링, 실제 충돌 침투 깊이 해석값 비교, 빈 목록 총 3개.

## 로드맵 전체 완료

`docs/brainstorming/충돌감지_알고리즘_20260720_2119.md`의 로드맵 10개 항목(통일 Geometry 입력 타입 → GJK+EPA → Sweep and Prune → BVH → R-Tree → Voxel/Spatial Hashing → Octree/k-d Tree → MPR → Conservative Advancement → 커스텀 FCL) 전부 구현 완료.

## 현재 상태에서 확인된 이슈

- [RESOLVED] `Win32WindowTest.PumpMessagesReturnsTrueWhenNoQuitPosted` 1건 실패 — 이전 사이클들에서도 동일하게 보고된 기존 이슈, 이번 사이클과 무관.
  → resolved: `tests/Win32WindowTest.cpp`에 픽스처 추가, SetUp()에서 잔여 WM_QUIT 드레인. 227/227 테스트 통과 확인 (2026-07-22).
- [DEFERRED] (item 8 MPR에서 이월) OBB 등 평평한(polytope) 도형에서 MPR 포탈 정제가 수렴하지 않는 알려진 한계 — `docs/commit/MPR_20260720_2259.md` 참고.
- [DEFERRED] (item 9 CCD에서 이월) Conservative Advancement가 경계 구 근사를 사용해 늘어진 도형에서 TOI가 실제보다 보수적으로 나올 수 있음 — `docs/commit/ConservativeAdvancement_20260720_2311.md` 참고.

## 체크리스트/검증 상태

전략 문서(`../strategy/FCL_20260720_2315.md`)의 체크리스트 4개 전부 `[x]`, 승인/검증 기준 3개 전부 충족.

## 다음 단계

사용자 지시에 따라, 로드맵 2~10번(item 1 Geometry는 이미 개별 리뷰 완료) 전체를 대상으로 한 번에 Stage 1(품질 점검) + Stage 2(다이어그램) 코드 리뷰를 진행한다.
