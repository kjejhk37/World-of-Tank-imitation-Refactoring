../strategy/Octree_KdTree_20260720_2254.md

# Commit: Octree / k-d Tree

## 구현 결과

- `src/collision/octree/Octree.h`/`.cpp` — 적응형 8분할. 객체는 "완전히 들어맞는 가장 깊은 자식"에만 배치(멀티 노드 등록 없음), 중심선을 걸치면 현재 노드에 유지. 쿼리는 (노드 내부 쌍) + (노드-자손 쌍) + (자식 재귀)로 중복/누락 없이 처리.
- `src/collision/kdtree/KdTree.h`/`.cpp` — BVH와 같은 이진 트리 + self-collide 패턴이지만, 분할 축을 깊이에 따라 순환(depth%3)한다는 점만 다름(BVH는 가장 넓은 축을 고름) — k-d Tree의 정체성에 해당하는 유일하지만 본질적인 차이. BVH를 include하지 않고 독립적으로 새로 구현.
- 두 모듈 모두 리프-리프 최종 판정은 기존 `geometry/Intersections.h`의 `Intersects(AABB, AABB)` 재사용.
- `CMakeLists.txt`: `app_lib`/`tests`에 각각 2개 `.cpp` 추가.
- `tests/OctreeTest.cpp`(3개), `tests/KdTreeTest.cpp`(3개): 겹침/비겹침 핵심 케이스, 빈 목록, Octree는 중심선 걸침 객체가 부모에 남아도 자손과의 겹침이 정상 검출되는지 별도 확인.
- 두 모듈 모두 BVH를 포함해 다른 알고리즘 모듈을 전혀 참조하지 않음(독립성 확인).

## 현재 상태에서 확인된 이슈

- [RESOLVED] `Win32WindowTest.PumpMessagesReturnsTrueWhenNoQuitPosted` 1건 실패 — 이전 사이클들에서도 동일하게 보고된 기존 이슈, 이번 사이클과 무관.
  → resolved: `tests/Win32WindowTest.cpp`에 픽스처 추가, SetUp()에서 잔여 WM_QUIT 드레인. 227/227 테스트 통과 확인 (2026-07-22).

## 체크리스트/검증 상태

전략 문서(`../strategy/Octree_KdTree_20260720_2254.md`)의 체크리스트 5개 전부 `[x]`, 승인/검증 기준 2개 전부 충족.

## 다음 단계

코드 리뷰는 생략(로드맵 10번까지 완료 후 일괄 진행), 로드맵 8번(MPR, 참고용 낮은 우선순위)으로 진행.
